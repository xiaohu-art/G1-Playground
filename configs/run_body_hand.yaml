defaults:
  - robot: g1
  - policy: body_hand_distill_largebox
  - motion: largebox_039_v00
  - deployment: sim
  - override hydra/job_logging: disabled
  - override hydra/hydra_logging: disabled
  - _self_

device: cpu

startup:
  ramp_seconds: 3.0
  # A dynamic HOI reference must never be frozen during a policy blend.
  blend_seconds: 0.0

recording:
  enabled: true
  directory: logs

env:
  _target_: g1_playground.g1_env.G1Env

hydra:
  job:
    chdir: false
  output_subdir: null
  run:
    dir: .
